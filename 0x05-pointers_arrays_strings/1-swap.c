#include "main.h"

/**
 * _strlen - returns the length of a string
 * @str: string to check
 * @b: second int
 *
 * Return: void
 */

void swap_int(int *a, int *b)
{
	int c;

	c = *a;
	*a = *b;
	*b = c;
}
